Index: core/src/main/resources/lib/hudson/queue.jelly
===================================================================
RCS file: /cvs/hudson/hudson/main/core/src/main/resources/lib/hudson/queue.jelly,v
retrieving revision 1.18
diff -u -8 -p -r1.18 queue.jelly
--- core/src/main/resources/lib/hudson/queue.jelly	12 Jan 2008 01:47:07 -0000	1.18
+++ core/src/main/resources/lib/hudson/queue.jelly	12 Mar 2008 08:59:14 -0000
@@ -2,35 +2,37 @@
   displays the build queue.
 
   Attributes:
     @items
       Queue items to be displayed. Normally ${app.queue.items}
 -->
 <j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">
   <l:pane title="${%Build Queue}" width="2" id="buildQueue">
+    <j:if test="${app.quietingDown}">
+      <tr>
+        <td class="pane" colspan="2" style="white-space: normal;">
+          ${%Hudson is going to shut down. No further builds will be performed.}
+          <j:if test="${h.checkPermission(app.ADMINISTER)}">
+            <a href="${rootURL}/cancelQuietDown">(${%cancel})</a>
+          </j:if>
+        </td>
+      </tr>
+    </j:if>
     <j:choose>
       <j:when test="${empty(items)}">
-        <tr>
-          <td class="pane" colspan="2">
-            ${%No builds in the queue.}
-          </td>
-        </tr>
-      </j:when>
-      <j:otherwise>
-        <j:if test="${app.quietingDown}">
+        <j:if test="${! app.quietingDown}">
           <tr>
-            <td class="pane" colspan="2" style="white-space: normal;">
-              ${%Hudson is going to shut down. No further builds will be performed.}
-              <j:if test="${h.checkPermission(app.ADMINISTER)}">
-                <a href="${rootURL}/cancelQuietDown">(${%cancel})</a>
-              </j:if>
+            <td class="pane" colspan="2">
+              ${%No builds in the queue.}
             </td>
           </tr>
-        </j:if>
+      </j:if>
+      </j:when>
+      <j:otherwise>
         <j:forEach var="item" items="${items}">
           <tr>
             <td class="pane" width="100%" tooltip="${item.why}">
               <a href="${rootURL}/${item.task.url}">
                 ${item.task.fullDisplayName}
               </a>
             </td>
             <td class="pane" width="16" align="center" valign="middle">
