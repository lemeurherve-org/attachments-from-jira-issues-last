package test;

public class PMDTest {

    public void someMethod() {

        String toTest = "ax";

        if (toTest.contains("Velšská premier") || toTest.contains("Celkové umístění"))
            return;

        if (toTest.contains("Velšská premier") || toTest.contains("Celkové umístění"))
            return;

        if (toTest.contains("Velšská premier") || toTest.contains("Celkové umístění"))
            return;

        if (toTest.contains("Velšská premier") || toTest.contains("Celkové umístění"))
            return;

    }

}
