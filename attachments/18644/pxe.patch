Index: pxeboot/tftpd/pom.xml
===================================================================
--- pxeboot/tftpd/pom.xml	(revision 18714)
+++ pxeboot/tftpd/pom.xml	(working copy)
@@ -4,10 +4,10 @@
   <parent>
     <artifactId>pxeboot</artifactId>
     <groupId>org.jvnet.hudson.pxeboot</groupId>
-    <version>1.2-SNAPSHOT</version>
+    <version>1.3-SNAPSHOT</version>
   </parent>
   <artifactId>tftpd</artifactId>
-  <version>1.2-SNAPSHOT</version>
+  <version>1.3-SNAPSHOT</version>
   <name>TFTP daemon</name>
 
   <build>
Index: pxeboot/pxe-boot-manager/pom.xml
===================================================================
--- pxeboot/pxe-boot-manager/pom.xml	(revision 18714)
+++ pxeboot/pxe-boot-manager/pom.xml	(working copy)
@@ -2,7 +2,7 @@
   <parent>
     <artifactId>pxeboot</artifactId>
     <groupId>org.jvnet.hudson.pxeboot</groupId>
-    <version>1.2-SNAPSHOT</version>
+    <version>1.3-SNAPSHOT</version>
   </parent>
   <modelVersion>4.0.0</modelVersion>
   <artifactId>pxe-boot-manager</artifactId>
@@ -36,12 +36,12 @@
     <dependency>
       <groupId>${project.groupId}</groupId>
       <artifactId>tftpd</artifactId>
-      <version>1.2-SNAPSHOT</version>
+	  <version>${project.version}</version>
     </dependency>
     <dependency>
       <groupId>${project.groupId}</groupId>
       <artifactId>proxy-dhcp</artifactId>
-      <version>1.2-SNAPSHOT</version>
+	  <version>${project.version}</version>
     </dependency>
   </dependencies>
 </project>
Index: pxeboot/io-utils/pom.xml
===================================================================
--- pxeboot/io-utils/pom.xml	(revision 18714)
+++ pxeboot/io-utils/pom.xml	(working copy)
@@ -2,7 +2,7 @@
   <parent>
     <artifactId>pxeboot</artifactId>
     <groupId>org.jvnet.hudson.pxeboot</groupId>
-    <version>1.2-SNAPSHOT</version>
+    <version>1.3-SNAPSHOT</version>
   </parent>
   <modelVersion>4.0.0</modelVersion>
   <groupId>org.jvnet.hudson.pxeboot</groupId>
Index: pxeboot/pom.xml
===================================================================
--- pxeboot/pom.xml	(revision 18714)
+++ pxeboot/pom.xml	(working copy)
@@ -10,7 +10,7 @@
   <artifactId>pxeboot</artifactId>
   <packaging>pom</packaging>
   <name>PXEBoot parent POM</name>
-  <version>1.2-SNAPSHOT</version>
+  <version>1.3-SNAPSHOT</version>
   
   <modules>
     <module>proxy-dhcp</module>
Index: pxeboot/hudson-proxy-dhcp/pom.xml
===================================================================
--- pxeboot/hudson-proxy-dhcp/pom.xml	(revision 18714)
+++ pxeboot/hudson-proxy-dhcp/pom.xml	(working copy)
@@ -4,7 +4,7 @@
   <parent>
     <artifactId>pxeboot</artifactId>
     <groupId>org.jvnet.hudson.pxeboot</groupId>
-    <version>1.2-SNAPSHOT</version>
+    <version>1.3-SNAPSHOT</version>
   </parent>
   <artifactId>hudson-proxy-dhcp</artifactId>
   
Index: pxeboot/proxy-dhcp/src/main/java/org/jvnet/hudson/proxy_dhcp/ProxyDhcpService.java
===================================================================
--- pxeboot/proxy-dhcp/src/main/java/org/jvnet/hudson/proxy_dhcp/ProxyDhcpService.java	(revision 18714)
+++ pxeboot/proxy-dhcp/src/main/java/org/jvnet/hudson/proxy_dhcp/ProxyDhcpService.java	(working copy)
@@ -26,6 +26,7 @@
     public final Inet4Address tftpServer;
     public final String bootFileName;
     private final DatagramSocket server;
+    private final DatagramSocket replySocket;
 
     public ProxyDhcpService(Inet4Address tftpServer, String bootFileName) throws SocketException {
         this(tftpServer,bootFileName,null);
@@ -38,6 +39,9 @@
         server = new DatagramSocket(DHCP_SERVER_PORT,interfaceToListen);
         server.setBroadcast(true);
 
+        replySocket = new DatagramSocket(0,tftpServer);
+        replySocket.setBroadcast(true);
+
         LOGGER.info("TFTP server: "+tftpServer.getHostAddress());
         LOGGER.info("Boot file: "+bootFileName);
     }
@@ -99,7 +103,7 @@
         datagram = reply.pack();
         datagram.setAddress(InetAddress.getByName("255.255.255.255"));
         datagram.setPort(DHCP_CLIENT_PORT);
-        server.send(datagram);
+        replySocket.send(datagram);
         LOGGER.fine("responded");
     }
 
Index: pxeboot/proxy-dhcp/pom.xml
===================================================================
--- pxeboot/proxy-dhcp/pom.xml	(revision 18714)
+++ pxeboot/proxy-dhcp/pom.xml	(working copy)
@@ -2,7 +2,7 @@
   <parent>
     <artifactId>pxeboot</artifactId>
     <groupId>org.jvnet.hudson.pxeboot</groupId>
-    <version>1.2-SNAPSHOT</version>
+    <version>1.3-SNAPSHOT</version>
   </parent>
   <modelVersion>4.0.0</modelVersion>
   <artifactId>proxy-dhcp</artifactId>
Index: plugins/pxe/pom.xml
===================================================================
--- plugins/pxe/pom.xml	(revision 18714)
+++ plugins/pxe/pom.xml	(working copy)
@@ -26,6 +26,6 @@
   </dependencies>
 
   <properties>
-    <pxeboot.version>1.0</pxeboot.version>
+    <pxeboot.version>1.3-SNAPSHOT</pxeboot.version>
   </properties>
 </project>
