Index: src/main/java/hudson/tasks/_ant/AntTargetNote.java
===================================================================
--- src/main/java/hudson/tasks/_ant/AntTargetNote.java	(revision 33373)
+++ src/main/java/hudson/tasks/_ant/AntTargetNote.java	(working copy)
@@ -44,7 +44,7 @@
         // still under development. too early to put into production
         if (!ENABLED)   return null;
 
-        MarkupText.SubText t = text.findToken(Pattern.compile("^[^:]+(?=:)"));
+        MarkupText.SubText t = text.findToken(Pattern.compile(".*(?=:)"));
         if (t!=null)
             t.addMarkup(0,t.length(),"<b class=ant-target>","</b>");
         return null;
