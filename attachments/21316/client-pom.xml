<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.magnetstreet</groupId>
  <artifactId>jagAccess-client</artifactId>
  <packaging>jar</packaging>
  <version>3.4-SNAPSHOT</version>
  <name>MagnetStreet Jag Access WS Client</name>
  <description>Auto-generated client for the JagAccess service</description>
  <url>http://magnetstreet.com/</url>
  
  <parent>
    <artifactId>mgst-super-pom</artifactId>
    <groupId>com.magnetstreet</groupId>
    <version>1.2-SNAPSHOT</version>
  </parent>

  <build>
    <finalName>jagAccess-client</finalName>
    <testResources>
      <testResource>
        <directory>src/test/resources</directory>
        <filtering>true</filtering>
      </testResource>
    </testResources>
    <resources>
      <resource>
        <directory>src/main/resources/</directory>
        <filtering>true</filtering>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>jaxws-maven-plugin</artifactId>
        <version>1.12</version>
        <executions>
          <execution>
            <phase>generate-sources</phase>
            <goals>
              <goal>wsimport</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <!-- <destDir>${project.build.directory}/jaxws/wsimport/classes</destDir> -->
          <extension>true</extension>
          <keep>true</keep>
          <sourceDestDir>${project.build.sourceDirectory}</sourceDestDir>
          <verbose>true</verbose>
          <wsdlUrls>
            <wsdlUrl>${jagAccess.ws.wsdl}</wsdlUrl>
          </wsdlUrls>
          <bindingFiles>
            <bindingFile>${basedir}/src/main/resources/JaxbBindings.xsd</bindingFile>
          </bindingFiles>
          <Xnocompile>true</Xnocompile>
        </configuration>
        
        <!-- if you want to use a specific version of JAX-WS, you can do so like this -->
        <!--<dependencies>
          <dependency>
            <groupId>com.sun.xml.ws</groupId>
            <artifactId>jaxws-tools</artifactId>
            <version>2.2.1</version>
          </dependency>
        </dependencies>-->
      </plugin>
      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <phase>generate-sources</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <tasks>
                <delete dir="${project.build.sourceDirectory}/com/magnetstreet/persistence/jag/object" />
              </tasks>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  
  <dependencies>
    <dependency>
      <groupId>com.magnetstreet</groupId>
      <artifactId>lib-model</artifactId>
      <version>2.0-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>com.magnetstreet</groupId>
      <artifactId>lib-dao</artifactId>
      <version>2.0-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>com.sun.xml.ws</groupId>
      <artifactId>jaxws-rt</artifactId>
      <version>2.2.1</version>
      <type>jar</type>
      <scope>compile</scope>
      <exclusions>
        <exclusion>
      	  <artifactId>activation</artifactId>
      	  <groupId>javax.activation</groupId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- test deps -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.7</version>
      <type>jar</type>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>