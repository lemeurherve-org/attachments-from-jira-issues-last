pipeline { 
  agent any
  parameters {
    booleanParam(name: 'PARAM', defaultValue: false, description: 'value')
    string (name: 'PARAM2', defaultValue: 'val2', description: 'value2')
  }
  stages {
    stage ('Build') {
      steps {
	sh "echo ${params.PARAM}"
	sh "echo ${params.PARAM2}"
      }
    }
  }
} //pipeline
