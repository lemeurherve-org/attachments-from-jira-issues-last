Only in /home/asa/Downloads/git-plugin-git-3.4.1_mod: JENKINS-48938.patch
diff -ru /home/asa/Downloads/git-plugin-git-3.4.1/src/main/java/hudson/plugins/git/GitSCM.java /home/asa/Downloads/git-plugin-git-3.4.1_mod/src/main/java/hudson/plugins/git/GitSCM.java
--- /home/asa/Downloads/git-plugin-git-3.4.1/src/main/java/hudson/plugins/git/GitSCM.java	2017-07-18 12:26:03.000000000 +0200
+++ /home/asa/Downloads/git-plugin-git-3.4.1_mod/src/main/java/hudson/plugins/git/GitSCM.java	2018-01-12 15:59:16.006807905 +0100
@@ -782,7 +782,7 @@
 
     @NonNull
     private BuildData fixNull(BuildData bd) {
-        return bd != null ? bd : new BuildData(getScmName(), getUserRemoteConfigs()) /*dummy*/;
+        return bd != null ? bd : new BuildData(getKey(), getUserRemoteConfigs()) /*dummy*/;
     }
 
     /**
@@ -1024,6 +1024,7 @@
         }
         Build revToBuild = new Build(marked, rev, build.getNumber(), null);
         buildData.saveBuild(revToBuild);
+	buildData.setScmName(getKey()); // !
 
         if (buildData.getBuildsByBranchName().size() >= 100) {
             log.println("JENKINS-19022: warning: possible memory leak due to Git plugin usage; see: https://wiki.jenkins-ci.org/display/JENKINS/Remove+Git+Plugin+BuildsByBranch+BuildData");
@@ -1687,10 +1688,9 @@
     public BuildData copyBuildData(Run build) {
         BuildData base = getBuildData(build);
         if (base==null)
-            return new BuildData(getScmName(), getUserRemoteConfigs());
+            return new BuildData(getKey(), getUserRemoteConfigs());
         else {
            BuildData buildData = base.clone();
-           buildData.setScmName(getScmName());
            return buildData;
         }
     }
@@ -1708,8 +1708,10 @@
             List<BuildData> buildDataList = build.getActions(BuildData.class);
             for (BuildData bd : buildDataList) {
                 if (bd != null && isRelevantBuildData(bd)) {
-                    buildData = bd;
-                    break;
+		    if (bd.getScmName().equals(getKey())) { // we need the BuildData which corresponds to the specific repository
+                        buildData = bd;
+                        break;
+		    }
                 }
             }
             if (buildData != null) {
diff -ru /home/asa/Downloads/git-plugin-git-3.4.1/src/main/java/hudson/plugins/git/util/BuildData.java /home/asa/Downloads/git-plugin-git-3.4.1_mod/src/main/java/hudson/plugins/git/util/BuildData.java
--- /home/asa/Downloads/git-plugin-git-3.4.1/src/main/java/hudson/plugins/git/util/BuildData.java	2017-07-18 12:26:03.000000000 +0200
+++ /home/asa/Downloads/git-plugin-git-3.4.1_mod/src/main/java/hudson/plugins/git/util/BuildData.java	2018-01-12 15:51:20.478818338 +0100
@@ -247,6 +247,7 @@
 
         clone.buildsByBranchName = new HashMap<>();
         clone.remoteUrls = new HashSet<>();
+        clone.scmName = scmName;
 
         for (Map.Entry<String, Build> buildByBranchName : buildsByBranchName.entrySet()) {
             String branchName = buildByBranchName.getKey();
